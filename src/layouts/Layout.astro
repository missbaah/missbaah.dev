---
// imports
import Nav from "../components/Nav.astro"
import { Font } from "astro:assets"
import "@radix-ui/themes/styles.css"
import "../styles/global.css"

const { pageTitle } = Astro.props
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="generator" content={Astro.generator} />
    <title>{pageTitle}</title>
    <Font cssVariable="--font-aeonik-pro" />
    <Font cssVariable="--font-inter" />
  </head>
  <body class="min-h-screen bg-cream text-text-color">
    <main class="flex flex-col h-full items-start px-4 md:px-[20%] md:py-8">
      <section>
        <Nav />
      </section>
      <hr class="w-full text-hairline" />
      <section class="">
        <slot />
      </section>
      <!-- <section class="lg:col-span-2 sticky top-16">crossword here</section> -->
    </main>
  </body>
</html>
