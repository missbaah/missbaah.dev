---
//imports
import clsx from "clsx"
import Footer from "./Footer.astro"

// js
const navItems = [
  { name: "Home", path: "/" },
  { name: "Projects", path: "/projects" },
  // { name: "Blog", path: "/blog" },
  // { name: "Notes", path: "/notes" },
  { name: "About", path: "/about" },
]
const { pathname } = Astro.url
---

<nav class="flex w-full pt-8.5 pb-6">
  <div class="flex md:gap-6 gap-3 justify-between">
    {
      navItems.map((item) => {
        return (
          <a
            class={clsx(
              {
                "after:w-full opacity-100": item.path === pathname,
              },
              "opacity-[0.45] relative after:absolute after:bottom-0 after:left-0 after:h-px after:bg-black after:w-0"
            )}
            href={item.path}
          >
            {item.name}
          </a>
        )
      })
    }
  </div>

  <!-- <Footer class="hidden lg:block" /> -->
</nav>
